<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0-android</TargetFramework>
    <SupportedOSPlatformVersion>21</SupportedOSPlatformVersion>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationId>com.kuttab.app</ApplicationId>
    <ApplicationVersion>81</ApplicationVersion>
    <ApplicationDisplayVersion>0.81</ApplicationDisplayVersion>
    <!-- RuntimeIdentifiers removed to build universal APK -->
    <EmbedAssembliesIntoApk>true</EmbedAssembliesIntoApk>
    <!-- Enable assembly compression to reduce APK size -->
    <AndroidEnableAssemblyCompression>true</AndroidEnableAssemblyCompression>
    <AndroidUseFastDeployment>false</AndroidUseFastDeployment>
    <!-- Disable AOT compilation to avoid SDK resolution issues -->
    <RunAOTCompilation>false</RunAOTCompilation>
    <EnableLLVM>false</EnableLLVM>
    <!-- Keep linking disabled to prevent issues -->
    <PublishTrimmed>false</PublishTrimmed>
    <TrimMode>partial</TrimMode>
    <AndroidLinkMode>None</AndroidLinkMode>
    <!-- Strip debug symbols in release -->
    <DebugSymbols>false</DebugSymbols>
    <DebugType>none</DebugType>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Kuttab.Core/Kuttab.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Xamarin.AndroidX.AppCompat" Version="1.7.0.1" />
    <PackageReference Include="Xamarin.AndroidX.RecyclerView" Version="1.3.2.6" />
    <PackageReference Include="Xamarin.Google.Android.Material" Version="1.11.0.1" />
  </ItemGroup>

  <ItemGroup>
    <AndroidAsset Include="Assets\**" />
    <!-- Include localization JSON files as Android assets -->
    <AndroidAsset Include="..\Localization\*.json">
      <Link>Localization/%(FileName)%(Extension)</Link>
    </AndroidAsset>
  </ItemGroup>
</Project>