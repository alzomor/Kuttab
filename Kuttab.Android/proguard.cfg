# ProGuard/R8 configuration for Kuttab Android app

# Keep all classes in the main application
-keep class com.kuttab.app.** { *; }

# Keep activities, services, receivers, providers
-keep public class * extends android.app.Activity
-keep public class * extends android.app.Service
-keep public class * extends android.content.BroadcastReceiver
-keep public class * extends android.content.ContentProvider

# Keep all native methods
-keepclasseswithmembernames class * {
    native <methods>;
}

# Keep all custom views
-keep public class * extends android.view.View {
    public <init>(android.content.Context);
    public <init>(android.content.Context, android.util.AttributeSet);
    public <init>(android.content.Context, android.util.AttributeSet, int);
    public void set*(...);
    *** get*();
}

# Keep all model classes
-keep class * implements java.io.Serializable { *; }
-keepclassmembers class * implements java.io.Serializable {
    static final long serialVersionUID;
    private static final java.io.ObjectStreamField[] serialPersistentFields;
    private void writeObject(java.io.ObjectOutputStream);
    private void readObject(java.io.ObjectInputStream);
    java.lang.Object writeReplace();
    java.lang.Object readResolve();
}

# Keep all enums
-keepclassmembers enum * {
    public static **[] values();
    public static ** valueOf(java.lang.String);
}

# Keep all interfaces
-keep interface * { *; }

# Keep all annotations
-keepattributes *Annotation*

# Keep all inner classes
-keepattributes InnerClasses

# Keep all signatures
-keepattributes Signature

# Keep line numbers for debugging
-keepattributes SourceFile,LineNumberTable

# Keep all JavaScript interface methods
-keepclassmembers class * {
    @android.webkit.JavascriptInterface <methods>;
}

# Keep all Xamarin generated classes
-keep class md5* { *; }
-keep class mono.* { *; }
-keep class com.android.* { *; }

# Preserve line numbers and source file names for better debugging
-keepattributes SourceFile,LineNumberTable

# Keep all public methods that could be called via reflection
-keepclassmembers class * {
    public <methods>;
}

# Keep all fields that could be accessed via reflection
-keepclassmembers class * {
    public <fields>;
}
